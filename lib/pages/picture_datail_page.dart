import 'package:flutter/material.dart';

class PictureDetailPage extends StatelessWidget {
  const PictureDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('detalhes da imagem'),
      ),
      body: const Text('detalhes da imagem'),
    );
  }
}
